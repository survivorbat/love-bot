{
  "name": "discordbot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf './dist'",
    "compile": "npm run clean && tsc",
    "start": "npm run compile && node dist/index.js",
    "docker": "docker build . -t lovebot && docker run -e DISCORD_TOKEN=${DISCORD_TOKEN} -name discordbot -d lovebot"
  },
  "author": "survivorbat",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^13.7.0"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.6.4"
  }
}
